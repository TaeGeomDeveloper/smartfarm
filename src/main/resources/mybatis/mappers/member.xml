<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.member">
	<resultMap id="memResult" type="memberVO">
		<id property="id" column="id" />
        <result property="pwd" column="pwd" />
        <result property="name" column="name" />
        <result property="email" column="email" />
        <result property="b_num" column="b_num" />
        <result property="phone" column="phone" />
        <result property="l_phone" column="l_phone" />
        <result property="address" column="address" />
        <result property="d_address" column="d_address"/>
        <result property="postal_code" column="postal_code" />
        <result property="r_num" column="r_num" />
        <result property="member_type" column="member_type" />
        <result property="business" column="business" />
        <result property="join_date" column="join_date" />
	</resultMap> 
	

	<select id="selectAllMemberList" resultMap="memResult">
      <![CDATA[
         select * from member order by join_date desc	 	
      ]]>
	</select>
	
	 
	<select id="selectMemberById" resultType="memberVO"  parameterType="String" >
      <![CDATA[
         select * from member
         where
         id=#{id}			
      ]]>
	 </select>	
	 
	 <select id="selectMemberByPwd" resultType="memberVO"  parameterType="String" >
      <![CDATA[
         select * from member
         where
         pwd=#{pwd}			
      ]]>
	 </select>
    
     <insert id="insertMember"  parameterType="memberVO">
		<![CDATA[
		 insert into member(id,pwd, name, email, b_num, phone, l_phone, address, d_address, postal_code, r_num, member_type, business, join_date)
		 values(#{id}, #{pwd}, #{name}, #{email}, #{b_num}, #{phone}, #{l_phone}, #{address}, #{d_address}, #{postal_code}, #{r_num}, #{member_type}, #{business}, now())
		]]>      
	</insert>
	
   
   <update id="updateMember"  parameterType="memberVO">
     <![CDATA[
	     update member
	     set pwd=#{pwd}, email=#{email}, b_num=#{b_num}, phone=#{phone}, l_phone=#{l_phone}, address=#{address}, d_address=#{d_address}, postal_code=#{postal_code}, business=#{business}
	     where
	     id=#{id}
      ]]>      
   </update> 
     
   <delete id="deleteMember"  parameterType="String">
	<![CDATA[
	   delete from  member
	   where
	   id=#{id}
	]]>      
  </delete>
	
  <select id="findMember" resultType="memberVO"  parameterType="java.util.Map" >
      <![CDATA[
         select * from member
         where
         id=#{id} and pwd = #{pwd}			
      ]]>
  </select>	
	
</mapper>